---
'@geonovum/standards-checker': patch
---

Inset the JSON/YAML format toggle by the editor's vertical scrollbar width so it no longer overlaps the scrollbar track on platforms with classic (non-overlay) scrollbars. The measurement ships as a new `useScrollbarWidth(view)` hook exported from `@geonovum/standards-checker/ui`.
